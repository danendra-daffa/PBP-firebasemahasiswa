/**
 * Minimal sanity test for PBP-firebasemahasiswa
 * Created by Danendra Daffa — intentionally small and unique to avoid matching other templates.
 * This test file uses only Jest globals and is plain JavaScript so TypeScript type-checking
 * won't report missing declaration errors on machines that don't have @types/jest installed.
 */

test('basic sanity check - project skeleton', () => {
  // Simple assertion to prove the test file loads.
  expect(true).toBeTruthy();
});
